#include "main.h"
/**
 * mul - multiplies two digits
 * @a: a digit to be multiplied
 * @b: another digit to be multiplied
 * Return: returns the value after multiplication
 */
int mul(int a, int b)
{
	int d;

	d = a * b;
	return (d);
}
